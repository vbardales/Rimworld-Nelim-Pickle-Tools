# RIMMSQOL steps in game, 2026-09-21

The standalone `RimmsqolSteps` companion, played through the `avec-rimmsqol` pass of FlavorTextExtendedFR
(features 12 to 15, tagged `@wip @rimmsqol` there), one lock, four launches in a `-Then` chain, headless in WSL.
Local files: `RimmsqolSteps/evidence/2026-09-21/` and `2026-09-21-replay/` (no longer tracked).

| Run | Identifiers | Launch | Feature | Result | Scenarios |
|---|---|---|---|---|---|
| 21:37 | `nelim.pickleshared.rimmsqol` | 1 | 12-rimmsqol-shortcut | passed, 3/3 | RIMMSQOL's own list offers the shortcut, hidden, and the bar does not draw it; revealed in RIMMSQOL the shortcut is drawn and opens the same page as Mod options; hidden again the shortcut leaves the bar and forgetting the choice leaves nothing behind |
| 21:37 | same | 2 | 13-rimmsqol-restart-reveal | passed, 1/1 | RIMMSQOL reveals the shortcut and the choice is kept (the settings file entry reads visible) |
| 21:37 | same | 3 | 14-rimmsqol-restart-hide | passed, 1/1 | the revealed shortcut survived the restart, then RIMMSQOL hides it again |
| 21:37 | same | 4 | 15-rimmsqol-restart-forget | passed, 1/1 | hiding the shortcut survived the restart, and RIMMSQOL is put back |
| 23:12 to 23:18 | `nelim.pickletools.rimmsqol` (renamed build) | 1 to 4 | 12, 13, 14, 15 | every launch `exitReason: passed`, 3/3 then 1/1 each | the same scenarios; the logs name `Nelim.PickleTools.Rimmsqol`; the WSL profile was left with `<mainButtons />` and no marker |

Four captures were taken in the first run (RIMMSQOL's list of main buttons with the Flavor Text shortcut, its edit
page with Visible ticked, before and after a restart, and the mod's own settings page opened by the revealed
button). They showed the path ran; they do not show that RIMMSQOL's checkbox is wired to the calls the steps make.

**Not shown**: a real click on RIMMSQOL's checkbox (the wiring is read from its source); other customisation mods;
the French interface; the bundle build (`nelim.pickletools`), which has only the English bridge probe in
[`aggregate.md`](aggregate.md).
