# Pending at Pickle

PreTest baseline update, 2026-09-23: Pickle v4.9.1 is the baseline (v4.9.0 added `!` exclusions to the run filter, a
change of upstream's own, not one of our PRs; v4.9.1 fixes camera steps in the world view). Our nine open PRs (#19, #21,
#22, #23, #28, #31, #32, #33, #34) were still open when this was written; only #20 is merged. Details in `TESTING.md`.

PreTest baseline update, 2026-09-22: Pickle v4.8.4 has been released with PR #20. The aggregate
PickleTools tests are queued against its official release archive; the rows below retain their own
recorded evidence dates and must not be read as v4.8.4 game validation.

State as last seen, 2026-09-21. The three rows on PR 23, the six PRs together and the bundles are as reported by the ArchitectStudio session, who measured them; they are not measurements of mine. A row says what was actually done, not what is hoped.

| Item | Upstream | State | Proof | Next |
|---|---|---|---|---|
| `no warnings from mod {string}` compared the packageId to a display name and passed vacuously; plus the positive step `a warning from mod {string} was logged` | issue #27, PR #28 | PR open. Kept as `patches/pr-28/` (4 commits, `git am`) | `warning-steps.feature` played in the headless WSL install, 5 scenarios, 5 passed; `dotnet test` green; step docs check clean | wait for review |
| Dashboard translated with no active language (NRE) and a snapshot guard | PR #22 | PR open. Kept as `patches/pr-22/` (5 commits, the REMOTE branch; the local branch of the writing session had diverged) | its language scenario is still `@wip`. Two earlier runs failed for reasons of my own test. Then, on the fixed build (2026-09-21 19:49, headless WSL, French), both scenarios passed: 2 passed, 0 failed, the first in 8.2 s. The comparison on the UNFIXED build, which would show that the scenario fails without the fix, has not been seen yet: its run is queued | prove the scenario on a fixed and an unfixed build, then decide: remove `@wip` or remove the scenario. Rebase onto the remote branch before any push |
| `I click button keyed {string}`: click a button by the translation key its label comes from, so a scenario runs in any language | PR #19 | PR open, checks green at last look (2026-09-21), awaiting review. Kept as `patches/pr-19/` (2 commits, `git am`), and **duplicated as `../KeyedClick/`** (the same step under the `Nelim's Pickle Tools:` prefix), compiled with 0 warnings, its pattern checked for ambiguity, **the copy never played** | `entry-flow.feature` played in the headless WSL install (Linux, Xvfb) on a local build that also carries other open branches: in English 3 passed; in French the two scenarios that spell `New colony` out failed with `known tags: btn:Nouvelle colonie, ...` and the keyed one passed. The two-language table is posted on the PR | keep the two versions in step until it merges: a change asked for in review is made in both. When it lands and a release carries it, delete `KeyedClick/` and `patches/pr-19/`, and change the prefix in the scenarios that use it |
| `the screen is clear` and `windows are allowed to open again`: drop the windows Pickle does not own as they open, for the length of a scenario, lifted from `[AfterScenario]` too; the decision lives in `WindowSuppressionRule` in Pickle.Core | PR #21 | PR open, 5 commits, head `e9f893c`, its analysis running at last look (the previous head was green). Kept as `patches/pr-21/` (`git am`), and **duplicated as `../ClearScreen/`** (the same two steps under the `Nelim's Pickle Tools:` prefix, with a prefix of its own on `WindowStack.Add` since Pickle's hook is out of a companion mod's reach), compiled with 0 warnings, its patterns checked for ambiguity, its four-scenario probe in `PickleToolsCheck` written, **the copy never played**; both are kept in step until the PR merges | the 4 scenarios of `window-suppression.feature` played headless in the WSL install on a local build at `ab2aa2e` (before the rebase and before the rule refactor): 4 passed, 0 failed, 0 skipped. `dotnet test` 186/186 on `e9f893c`, analyzer gate clean, the feature read by Pickle's own Gherkin parser (5 scenarios). **The scenarios have not been re-played on `e9f893c`, and the fifth, added with the documentation fix, has never been played**: two runs are queued, the four on a local build at `9ab3686` and the probe of the copy on the stock Pickle. The results of the first four were posted as a comment on the PR | wait for review. Its commits carry `Co-Authored-By` and not the `AI-assisted: <tool>` trailer CONTRIBUTING mentions; Virginie decided on 2026-09-21 that the co-author line is enough, so the branch is not rewritten |
| Every click missed at an interface scale other than 100: `TagStore.Record` stored `GUIUtility.GUIToScreenRect`, which adds the clip origin unscaled and the local offset scaled, and `InputBackends.ToScreen` then scaled it again. Plus the step `the interface scale is {int} percent` and a regression scenario | PR #23 | PR open. Kept as `patches/pr-23/` (3 commits, `git am`, rebased onto `b3fda03`). Applying the series to a clean checkout of `b3fda03` gives tree `42302b84`, the same as the PR head `a7c8ccf` | measured in a running game at 1920x1080 and 150%: clip origin `(0, 375)` and local `y 285` stored as `802.5`, true GUI `660`. Headless WSL install, same mods and staging, only `TagStore.cs` differing: the regression scenario **passes** on the branch and **fails** with `GUIToScreenRect` (`window 'Page_SelectScenario' should be open`, no exception, the button being mid-menu). Architect Studio's scenario 16 at 150% passes on the branch and fails on the Workshop build (`the pointer never reached (50.25, 814.00)`). `dotnet test` 181 green. **The upstream CI has not run**: a fork PR waits for the maintainer to approve its workflows | wait for review. **Kept in step with the PR**: after any push to `fix/tag-rect-interface-scale` (a rebase included), regenerate with `git format-patch b3fda03..HEAD` or the new base, rewrite `BASE.txt`, and re-run the tree check. The fix is in `Mods\Pickle-local` and in the WSL run of Architect Studio through `-PickleSrc`; a Workshop Pickle still has the defect |
| Same defect as PR #23, played live over the Workshop Pickle: `InterfaceScale/` in PickleTools carries the step and a Harmony postfix on `TagStore.Record` that repairs the rect it just stored, put in place by a `[BeforeScenario]` hook | PR #23 (this is a live copy, not a patch series) | Tool built, 0 warnings. `nelim.pickletools.interfacescale`, staged with `wsl-deps.interfacescale.map` in `PickleToolsCheck/` | played headless on the WSL install, 2026-09-22, against the Workshop Pickle staged as-is (no `override:` in the staging log): `interface-scale.feature`, 1 passed, `exitReason: passed`. Not yet measured: the same feature with the tool NOT staged, as a negative control against the unfixed run already recorded above | the negative control, then decide whether it is worth keeping past the merge given the bundle scripts now prove the same PR by rebuilding Pickle instead. **When PR #23 merges**: delete `InterfaceScale/`, its row here, the `avec-pickletools` pass line wherever added, and change the one prefix in scenarios that used the step |
| `-pickle-fail-fast`: stop at the first failed scenario, report the rest as skipped, exit 1 | none yet | patch `0001`, compiled, **never played** | `tests/fail-fast-probe.feature` (pass, fail, pass) run with and without the flag: two runs queued | play both; expect 1 passed, 1 failed, 1 skipped against 2 passed and 1 failed. Then an issue first, not a PR |
| `@film-ticks:N`: film by game ticks instead of the clock (`FilmTickGate` in Pickle.Core, read on every rendered frame) | none yet | patch `0002`, compiled with 0 warnings, 188 tests pass (7 new); the tag filmed once in a WSL run on a local build (141 frames, one webm); the same feature as two steps ran on the Workshop Pickle: 56 pictures for about 90 ticks (about one per 1.6 ticks under the software renderer) | `Tests/`, and `AnimaSong/Tests/Pickle` scenario "the halo filmed one picture per tick"; **the alignment of pictures to ticks inside a game is not checked**, only the rule is | branch `feat/film-every-n-ticks` in `Documentspickle-localnima-film`; not pushed, no issue or PR. FilmTicks/ carries the same feature as two steps until it lands. CONTRIBUTING wants an `AI-assisted` trailer, already on the commit |
| `-pickle-include-wip` "truncates a run to its first feature" | issue #26 | **closed** on 2026-09-21 at Virginie's request, with a correcting comment: the diagnosis was retracted by its author, the run measured had been frozen by the machine sleeping | AUDIT.md lines on the signs of a sleep freeze; `-IncludeWip` seen playing `@wip` scenarios with a filter (language-steps.feature, 2 of 2 played) | nothing |
| A step for errors logged since startup (`no errors were logged` only covers the scenario) | none | idea, from the Flavor Text Extended session | its Player.log shows a load-time `[ERROR]` while that step passes; Pickle itself logs "N error(s) were logged outside any scenario and failed nothing" | read `LogWatch` to see whether it keeps errors from before it is armed |
| A step that waits until a tag's rect has stood still for N frames before a click: `I wait until button {string} stands still` and `I wait until tag {string} stands still` | PR #34 (RimWorks/Rimworld-Pickle), branch `feat/button-stands-still` on the fork, commit `a1293b0` | PR open. Kept as `patches/pr-34/` (1 commit, `git apply --ignore-whitespace`, checked against `b3fda03`: applying it to a fresh checkout gives tree `de62d57`, identical to the PR head's tree). `ClickDiagnostics/` keeps its own copy, `the button keyed {string} has stood still` (12 *repeats*, so 13 frames, one more than the PR's 12 *frames*; not reconciled) | `dotnet build` (whole solution, `--no-incremental`) 0 warnings; `dotnet test` 189/189, including 8 new `StillnessTrackerTests`; `check-step-docs.py` 203/203. **Not yet played in a game**: `Pickle/Features/still-steps.feature` (3 scenarios, a fixture window whose button drifts for 20 frames) has not been run through the headless WSL harness. The trace this was written from (WorkStudio's Work Tab case, button at raw x 975 for frames 216-225, 1748 from frame 226) is the same one recorded lower in this table, "Tag rect recorded 773 px off", closed as not a defect | play `still-steps.feature` headless (`-Mod PickleTools -PickleSrc <this build>`, `QuietNewFactions` left the monorepo and can no longer host it), post the result on the PR, then wait for review. Decide whether `ClickDiagnostics/`'s frame count should match the PR's before or after it lands |
| A lost-click report: when a click is followed by a `window {string} is open` that fails, print the pointer, the buttons drawn under it (image buttons included) and the window stack with each window's assembly, `absorbInputAroundWindow`, `GetsInput` and, for an `ImmediateWindow`, the method that draws it | none yet | Kept as `patches/0003-feat-steps-say-what-a-click-met...patch` and `patches/0004-feat-input-record-image-buttons...patch` (`git am`, checked against `b3fda03`: applying the series to a fresh checkout gives tree `3deb3a6d`, identical to the branch head `7a7a766` on `vbardales/Rimworld-Pickle`, not pushed). The report is appended to `window {string} is open`'s own failure message (`PickleContext.DescribeLastClick()`), the least intrusive of the three placements; image-button recording (`Widgets.ButtonImage*`, both patch backends) is the separable second patch. `dotnet build Pickle.slnx` 0 warnings, `dotnet test` 192 passed (11 new, unit testing the report's formatting only). **Not played**: `Upstream/tests/lost-click-probe/` exists to provoke the failure, but two WSL attempts did not complete — the first run's queue ticket went stale (32 ahead) and was reaped before its turn; the second took the lock but was stopped by hand, before staging, on Virginie's word to stop taking tickets meanwhile. `ClickDiagnostics/` carries the same report inside `I click the button keyed ... and the window ... opens`, compiled, also never played | Work Studio's Work Tab case again: the report said `open windows: ImmediateWindow, ImmediateWindow`, which names nothing, and three explanations were then made and refuted by measurement. `ImmediateWindow.doWindowFunc.Method` is what identifies one | Play `lost-click-probe.feature` in the WSL install once tickets are allowed again, read the report, then push the branch and open the PR describing both placements and asking the maintainers to choose; whether recording image buttons is acceptable overhead in Pickle itself (here it is a Harmony postfix/Concord injection installed on demand) |
| Tag rect recorded 773 px off for `btn:Work types...` | none | **closed, not a defect** | trace build: Pickle's and the probe's raw and converted rects agreed frame by frame; the layout changed mid-click | nothing |
| `{string} body type is {word}`, `{string} body is drawn from {string}`, `{string} apparel {string} is drawn from {string}`: set a pawn's body type and read the texture it is drawn with | PR #32 on `vbardales/Rimworld-Pickle`, branch `feat/body-type-step` | PR open and no longer a draft as checked on GitHub 2026-09-22; CI green at the earlier recorded check | played in the headless WSL install on a local build: a body-type scenario and an apparel scenario passed; a 10-case matrix (man, woman, boy, girl x Fat, Thin, Hulk, standard) passed for the 8 adult cases, and its 2 child cases failed on a scenario of my own (a pawn generated adult and aged to 8 wears adult apparel, which has no `_Child` texture); the fixed matrix was queued at the previous check. `Apparel_Pants` has no `wornGraphicPath` in Core 1.6.4871, so nothing is drawn for it | verify the fixed matrix result and follow review. Reported in `pickle-local/report-matrice` |
| `I open the {string} inspect tab` and `the {string} inspect tab is open`: open a pawn's inspect tabs (Gear, Bio, Health, Social, Needs, Log) and check which is open | PR #31 (RimWorks/Rimworld-Pickle), branch `feat/inspect-tab-steps` on the fork, commit `6eb53f4` | PR open, CI green, not yet reviewed. **Two versions, kept in step:** the pull request, and `InspectTabs/` here, the same logic under `Nelim's Pickle Tools: ` so it works on the stock Pickle | The PR's scenario `inspect tab steps open the pane tabs on a selected pawn` played in the headless WSL install on a local build (2026-09-21 12:04): 1 passed, all 12 steps, and the last film frame shows the pane on the Character tab. Played again inside the local integration build (15:11): 11 passed in the three features run. `dotnet build` 0 warnings, 181 tests pass, step docs check clean. `InspectTabs/`: compiled with 0 warnings, **never played yet**, run queued. **The failure paths (no match, ambiguous, hidden tab, nothing selected) have not been seen in a game** | Play `InspectTabs/` on the stock Pickle through `PickleToolsCheck`. Wait for review. When the PR merges and ships, delete `InspectTabs/` and this row |
| `Nelim's Pickle Tools: {string} xenotype is {string}` and `Nelim's Pickle Tools: a colonist {string} of kind {string} exists`: a Biotech xenotype on a pawn, and a colonist generated from any humanlike kind (the way to a mod's race, since a pawn's race cannot be swapped) | none yet | `ColonistRace/` (these two steps, a third that sets a certain body type, `Nelim's Pickle Tools: {string} body type is {word}`, and five that read a colonist back: gender, body type, xenotype, race, stage of life), compiled with 0 warnings | **Played on Pickle v4.9.1, 2026-09-24 and 2026-09-25**: `tools.feature` 4 of 4 (Genie gives Thin, Yttakin gives Hulk, a colonist of the vanilla kind `Villager`, gender and stage of life read back; the five tool features together 11 of 11 in English and in French) and `pickletools-colonistrace-bodytype.feature` 2 of 2 (a Hussar taken through Thin, Fat, Hulk and Male; a female pawn through Hulk and Female), after a first red run that showed a pawn with no body-type gene is not certain (`PawnGenerator.GetBodyTypeFor` then reads the adulthood backstory or draws Thin one time in two), so Male and Female add `Body_Standard`. Hussar is **not** deterministic by itself (see `ColonistRace/README.md`). **Not checked against Humanoid Alien Races: it is not installed here** | install HAR and try one of its kinds; then decide whether they earn a pull request |
| `I open the research tab {string}`, `the research window is on the tab {string}`, `the research window lists the project {string}`: open a tab of the research window by def name and read what it lists. `I click button` cannot: the window draws its tabs as `TabRecord`s through `TabDrawer`, which records no button tag | PR #33 (RimWorks/Rimworld-Pickle), branch `feat/research-tab-steps` on the fork, commit `580d5cf`, **out of draft on 2026-09-21 on Virginie's word** | PR open, **ready for review**. Sonar quality gate passed (0 new issues), CodeQL passed; **the build, xunit, Vale and lychee had not appeared at last look**, a fork PR waiting for the maintainer to approve its workflows. Kept as `patches/pr-33/` (1 commit, `git am`, tree `28d2ba23` identical to the PR head). **Three versions, kept in step:** the PR (def name only), `../ResearchSteps/` here (six texts with `PickleTools` in them, which also choose a tab or a project by its label and a tab by a translation key), and the five steps of `AdaptiveStorageNeolithicRenew/Tests/Pickle/Source/` | **The PR's own code has never been played**, nor its scenario in `ui-steps.feature` (`Main` and `Stonecutting`): `dotnet build Pickle.slnx` and `dotnet test` cannot run here (Pickle.Core targets net10.0, the SDK is 8.0). What ran: the mod's five steps, English 7 of 7 and French 9 of 11 (2 skipped), captures opened and validated by Virginie, who decided that stands for a human having played it; `ResearchSteps/` in its own pass, English 3 + 1 and French 3 + 2, all passed, the keyed capture opened in both languages. `check-step-docs.py` 204/204. The description of the PR says all this | wait for the CI and for review. A change asked in review is made in all three versions. When the PR merges and ships, delete `ResearchSteps/` and `patches/pr-33/`, move the features that use `PickleTools` texts to the official ones, and drop this row. `ResearchSteps/`'s label and key steps are not in the PR: Virginie decides whether they earn a follow-up |
| Pointer never reached its target at 150 % interface scale (`the pointer never reached (50.25, 814.00)`) | PR 23 | PR (not ours); played by ArchitectStudio against a bundle of this PR alone | `16-language-review.feature`: **3 of 3 passed, including the 150 % case** that failed with the Workshop Pickle on every run of the day. Its own report: the command line was checked for `-pickle-run=`. The proof is indirect: the failure disappears, and it could not have on the old code | wait for the merge; then the bundle folder is no longer needed |
| Several PRs together: 19, 21, 22, 23, 28, 31 | those PRs | played by ArchitectStudio as one bundle | feature 16 at 3 of 3, feature 02 (`02-architect-buttons.feature`) at 4 of 4, unit tests 181 of 181. **Not measured**: 19 and 21 alone against feature 02, the run was killed from outside after 61 s | none for us |
| Bundles: upstream `main` plus named PRs, built, unit-tested, staged as a mod folder and compared with the Workshop DLLs | none | tooling in `bundles/` (`make-bundle.sh <name> <PR>...`, `run-on-bundle.sh`), by ArchitectStudio | run from PickleTools: PR 23 alone 181/181; 19 + 21 186/186 with no conflict (upstream had moved); a refusal seen where expected on 31 + 33 (`ui-steps.feature`). A report is kept only when its `Player.log` carries our own `-pickle-run=` | see `bundles/README.md` |
| Sound in a `@film` video: record the game's audio alongside the filmstrip, so a person can hear a sound-bearing mod (EponaInstrumentsRenew's three carried instruments) from a report instead of on the Windows game | none: not sent, and per AUDIT.md it needs Virginie's agreement first | idea, from the EponaInstrumentsRenew session. **Built as an optional tool on 2026-09-24, `SoundCapture/` in this repository** (ffmpeg on the audio sink, no change to Pickle or the launcher, 13 unit tests green); **its in-game scenario ran on 2026-09-24 and heard nothing: the recorder measured -91 dB on the WSLg sink, and the cause is not established (the run never proved the game plays nothing, only that nothing reached the sink). A game-side check, in the manner of EponaInstrumentsRenew's, was added on 2026-09-25 (`the game is playing a sound`: a live sustainer, a playing one-shot or the menu music); its run is queued** | That session read Pickle's docs (steps, authoring, reports, autorun): no mention of audio, and `FilmEncoder` turns the JPEG filmstrip into a webm with ffmpeg, video only (not re-checked here). In the WSL install: `ffmpeg` present, `pactl` and `pulseaudio` not installed, WSLg exposes a PulseAudio socket (`/mnt/wslg/PulseServer`). Whether the game opens an audio output under Xvfb is **unknown**, and it decides everything | Cheapest trial, in WSL only: install PulseAudio's tools, create a null sink, record its monitor with `ffmpeg -f pulse` during a short run of a scenario that plays a sound. If the game produces no stream, stop. If it does: a harness option in `Headless/` first (nothing in Pickle changes), an upstream change only if Virginie asks for it (Unity mix captured with `OnAudioFilterRead` between the two film steps) |

## Patch series

`patches/pr-34`, `patches/pr-33`, `patches/pr-28`, `patches/pr-23`, `patches/pr-22` and `patches/pr-21` are `git format-patch` series against upstream `b3fda03`. All were checked by applying them to a fresh checkout of that commit: the resulting tree is identical to the tree of the PR branch on the fork. A `BASE.txt` in each says which head it is. When upstream merges a PR, delete its folder and its row here. `patches/pr-19` is against `67b2b38` instead, because it was made after upstream `main` had moved (its `BASE.txt` says how it was checked), and it has a second copy in `KeyedClick/` that is maintained beside it until the pull request lands. `patches/pr-21` likewise has a second copy in `ClearScreen/`.

`pr-21` is the one kept in step by hand: whenever `feat/clear-the-screen` moves, make the same change in `ClearScreen/` (its README names the mirrored commit), and delete the old files in its folder, run `git format-patch b3fda03..<head> -o Upstream/patches/pr-21` from a checkout of the branch (use the new base if upstream `main` has moved), rewrite `BASE.txt`, apply the series to a fresh checkout of the base and check that its tree equals the branch head's, and update the row above.

## Not here

Recording unnamed controls (`ButtonImage`, `ButtonInvisible`, `Checkbox`) so a click lost to one can be
explained: a design choice for upstream, raised once as a suspicion for the Work Tab case and not supported by
that case's numbers. It stays a possible issue for Virginie to decide.
