# Pending at Pickle

State as last seen, 2026-09-21. A row says what was actually done, not what is hoped.

| Item | Upstream | State | Proof | Next |
|---|---|---|---|---|
| `no warnings from mod {string}` compared the packageId to a display name and passed vacuously; plus the positive step `a warning from mod {string} was logged` | issue #27, PR #28 | PR open. Kept as `patches/pr-28/` (4 commits, `git am`) | `warning-steps.feature` played in the headless WSL install, 5 scenarios, 5 passed; `dotnet test` green; step docs check clean | wait for review |
| Dashboard translated with no active language (NRE) and a snapshot guard | PR #22 | PR open. Kept as `patches/pr-22/` (5 commits, the REMOTE branch; the local branch of the writing session had diverged) | its language scenario is still `@wip`. Two earlier runs failed for reasons of my own test. Then, on the fixed build (2026-09-21 19:49, headless WSL, French), both scenarios passed: 2 passed, 0 failed, the first in 8.2 s. The comparison on the UNFIXED build, which would show that the scenario fails without the fix, has not been seen yet: its run is queued | prove the scenario on a fixed and an unfixed build, then decide: remove `@wip` or remove the scenario. Rebase onto the remote branch before any push |
| `I click button keyed {string}`: click a button by the translation key its label comes from, so a scenario runs in any language | PR #19 | PR open, checks green at last look (2026-09-21), awaiting review. Kept as `patches/pr-19/` (2 commits, `git am`), and **duplicated as `../KeyedClick/`** (the same step under the `Nelim's Pickle Tools:` prefix), compiled with 0 warnings, its pattern checked for ambiguity, **the copy never played** | `entry-flow.feature` played in the headless WSL install (Linux, Xvfb) on a local build that also carries other open branches: in English 3 passed; in French the two scenarios that spell `New colony` out failed with `known tags: btn:Nouvelle colonie, ...` and the keyed one passed. The two-language table is posted on the PR | keep the two versions in step until it merges: a change asked for in review is made in both. When it lands and a release carries it, delete `KeyedClick/` and `patches/pr-19/`, and change the prefix in the scenarios that use it |
| `the screen is clear` and `windows are allowed to open again`: drop the windows Pickle does not own as they open, for the length of a scenario, lifted from `[AfterScenario]` too; the decision lives in `WindowSuppressionRule` in Pickle.Core | PR #21 | PR open, 4 commits, head `9ab3686`, CI green. Kept as `patches/pr-21/` (`git am`) and kept in step with the branch until it merges | the 4 scenarios of `window-suppression.feature` played headless in the WSL install on a local build at `ab2aa2e` (before the rebase and before the rule refactor): 4 passed, 0 failed, 0 skipped. `dotnet test` 186/186 on `9ab3686`, analyzer gate clean. **The scenarios have not been re-played on `9ab3686`.** Results posted as a comment on the PR | wait for review. Its commits carry `Co-Authored-By` and not the `AI-assisted: <tool>` trailer CONTRIBUTING mentions; Virginie decided on 2026-09-21 that the co-author line is enough, so the branch is not rewritten |
| Every click missed at an interface scale other than 100: `TagStore.Record` stored `GUIUtility.GUIToScreenRect`, which adds the clip origin unscaled and the local offset scaled, and `InputBackends.ToScreen` then scaled it again. Plus the step `the interface scale is {int} percent` and a regression scenario | PR #23 | PR open. Kept as `patches/pr-23/` (3 commits, `git am`, rebased onto `b3fda03`). Applying the series to a clean checkout of `b3fda03` gives tree `42302b84`, the same as the PR head `a7c8ccf` | measured in a running game at 1920x1080 and 150%: clip origin `(0, 375)` and local `y 285` stored as `802.5`, true GUI `660`. Headless WSL install, same mods and staging, only `TagStore.cs` differing: the regression scenario **passes** on the branch and **fails** with `GUIToScreenRect` (`window 'Page_SelectScenario' should be open`, no exception, the button being mid-menu). Architect Studio's scenario 16 at 150% passes on the branch and fails on the Workshop build (`the pointer never reached (50.25, 814.00)`). `dotnet test` 181 green. **The upstream CI has not run**: a fork PR waits for the maintainer to approve its workflows | wait for review. **Kept in step with the PR**: after any push to `fix/tag-rect-interface-scale` (a rebase included), regenerate with `git format-patch b3fda03..HEAD` or the new base, rewrite `BASE.txt`, and re-run the tree check. The fix is in `Mods\Pickle-local` and in the WSL run of Architect Studio through `-PickleSrc`; a Workshop Pickle still has the defect |
| `-pickle-fail-fast`: stop at the first failed scenario, report the rest as skipped, exit 1 | none yet | patch `0001`, compiled, **never played** | `tests/fail-fast-probe.feature` (pass, fail, pass) run with and without the flag: two runs queued | play both; expect 1 passed, 1 failed, 1 skipped against 2 passed and 1 failed. Then an issue first, not a PR |
| `@film-ticks:N`: film by game ticks instead of the clock (`FilmTickGate` in Pickle.Core, read on every rendered frame) | none yet | patch `0002`, compiled with 0 warnings, 188 tests pass (7 new); the tag filmed once in a WSL run on a local build (141 frames, one webm); the same feature as two steps ran on the Workshop Pickle: 56 pictures for about 90 ticks (about one per 1.6 ticks under the software renderer) | `Tests/`, and `AnimaSong/Tests/Pickle` scenario "the halo filmed one picture per tick"; **the alignment of pictures to ticks inside a game is not checked**, only the rule is | branch `feat/film-every-n-ticks` in `Documentspickle-localnima-film`; not pushed, no issue or PR. FilmTicks/ carries the same feature as two steps until it lands. CONTRIBUTING wants an `AI-assisted` trailer, already on the commit |
| `-pickle-include-wip` "truncates a run to its first feature" | issue #26 | diagnosis **retracted** by its author: the run measured had been frozen by the machine sleeping | AUDIT.md lines on the signs of a sleep freeze | close when Virginie says so |
| A step for errors logged since startup (`no errors were logged` only covers the scenario) | none | idea, from the Flavor Text Extended session | its Player.log shows a load-time `[ERROR]` while that step passes; Pickle itself logs "N error(s) were logged outside any scenario and failed nothing" | read `LogWatch` to see whether it keeps errors from before it is armed |
| A step that waits until a tag's rect has stood still for N frames before a click (`the button {string} stands still`) | none yet | `ClickDiagnostics/` carries it as `the button keyed {string} has stood still`, compiled, **never played**. Virginie said on 2026-09-21 that Pickle should get a pull request; a chip session prepares it | WorkStudio's Work Tab case, measured by a trace build: the button moved from raw x 975 to 1748 about ten frames after the tab opened, between press and release, so no click counted. Pickle's own rectangle was right at every frame. The wait was written from that trace and not replayed | The pull request; the step's text and the frame count are for Pickle's maintainers to shape. Not a defect in Pickle: a convenience, and the PR must say so |
| A lost-click report: when a click is followed by a `window {string} is open` that fails, print the pointer, the buttons drawn under it (image buttons included) and the window stack with each window's assembly, `absorbInputAroundWindow`, `GetsInput` and, for an `ImmediateWindow`, the method that draws it | none yet | `ClickDiagnostics/` carries it inside `I click the button keyed ... and the window ... opens`, compiled, **never played**. A chip session prepares the pull request | Work Studio's Work Tab case again: the report said `open windows: ImmediateWindow, ImmediateWindow`, which names nothing, and three explanations were then made and refuted by measurement. `ImmediateWindow.doWindowFunc.Method` is what identifies one | Decide with the maintainers whether it belongs in the click step, the `is open` step or a separate diagnostic; whether recording image buttons is acceptable overhead in Pickle itself (here it is a Harmony postfix installed on demand) |
| Tag rect recorded 773 px off for `btn:Work types...` | none | **closed, not a defect** | trace build: Pickle's and the probe's raw and converted rects agreed frame by frame; the layout changed mid-click | nothing |
| `{string} body type is {word}`, `{string} body is drawn from {string}`, `{string} apparel {string} is drawn from {string}`: set a pawn's body type and read the texture it is drawn with | PR #32 (draft) on `vbardales/Rimworld-Pickle`, branch `feat/body-type-step` | PR open as a draft, CI green | played in the headless WSL install on a local build: a body-type scenario and an apparel scenario passed; a 10-case matrix (man, woman, boy, girl x Fat, Thin, Hulk, standard) passed for the 8 adult cases, and its 2 child cases failed on a scenario of my own (a pawn generated adult and aged to 8 wears adult apparel, which has no `_Child` texture); the fixed matrix is queued. `Apparel_Pants` has no `wornGraphicPath` in Core 1.6.4871, so nothing is drawn for it | the fixed matrix, then Virginie decides whether the draft comes out of draft. Reported in `pickle-local/report-matrice` |
| `I open the {string} inspect tab` and `the {string} inspect tab is open`: open a pawn's inspect tabs (Gear, Bio, Health, Social, Needs, Log) and check which is open | PR #31 (RimWorks/Rimworld-Pickle), branch `feat/inspect-tab-steps` on the fork, commit `6eb53f4` | PR open, CI green, not yet reviewed. **Two versions, kept in step:** the pull request, and `InspectTabs/` here, the same logic under `Nelim's Pickle Tools: ` so it works on the stock Pickle | The PR's scenario `inspect tab steps open the pane tabs on a selected pawn` played in the headless WSL install on a local build (2026-09-21 12:04): 1 passed, all 12 steps, and the last film frame shows the pane on the Character tab. Played again inside the local integration build (15:11): 11 passed in the three features run. `dotnet build` 0 warnings, 181 tests pass, step docs check clean. `InspectTabs/`: compiled with 0 warnings, **never played yet**, run queued. **The failure paths (no match, ambiguous, hidden tab, nothing selected) have not been seen in a game** | Play `InspectTabs/` on the stock Pickle through `PickleToolsCheck`. Wait for review. When the PR merges and ships, delete `InspectTabs/` and this row |
| `Nelim's Pickle Tools: {string} xenotype is {string}` and `Nelim's Pickle Tools: a colonist {string} of kind {string} exists`: a Biotech xenotype on a pawn, and a colonist generated from any humanlike kind (the way to a mod's race, since a pawn's race cannot be swapped) | none yet | `ColonistRace/`, compiled with 0 warnings, **never played** | `ColonistRace/README.md`; four scenarios queued (Genie gives Thin, Hussar gives Hulk, a body type set after the xenotype wins, a colonist of the vanilla kind `Villager`). **Not checked against Humanoid Alien Races: it is not installed here** | play them; install HAR and try one of its kinds; then decide whether they earn a pull request |
| `I open the research tab {string}`, `the research window is on the tab {string}`, `the research window lists the project {string}`: open a tab of the research window by def name and read what it lists. `I click button` cannot: the window draws its tabs as `TabRecord`s through `TabDrawer`, which records no button tag | PR #33 (RimWorks/Rimworld-Pickle), branch `feat/research-tab-steps` on the fork, commit `580d5cf`, **out of draft on 2026-09-21 on Virginie's word** | PR open, **ready for review**. Sonar quality gate passed (0 new issues), CodeQL passed; **the build, xunit, Vale and lychee had not appeared at last look**, a fork PR waiting for the maintainer to approve its workflows. Kept as `patches/pr-33/` (1 commit, `git am`, tree `28d2ba23` identical to the PR head). **Three versions, kept in step:** the PR (def name only), `../ResearchSteps/` here (six texts with `PickleTools` in them, which also choose a tab or a project by its label and a tab by a translation key), and the five steps of `AdaptiveStorageNeolithicRenew/Tests/Pickle/Source/` | **The PR's own code has never been played**, nor its scenario in `ui-steps.feature` (`Main` and `Stonecutting`): `dotnet build Pickle.slnx` and `dotnet test` cannot run here (Pickle.Core targets net10.0, the SDK is 8.0). What ran: the mod's five steps, English 7 of 7 and French 9 of 11 (2 skipped), captures opened and validated by Virginie, who decided that stands for a human having played it; `ResearchSteps/` in its own pass, English 3 + 1 and French 3 + 2, all passed, the keyed capture opened in both languages. `check-step-docs.py` 204/204. The description of the PR says all this | wait for the CI and for review. A change asked in review is made in all three versions. When the PR merges and ships, delete `ResearchSteps/` and `patches/pr-33/`, move the features that use `PickleTools` texts to the official ones, and drop this row. `ResearchSteps/`'s label and key steps are not in the PR: Virginie decides whether they earn a follow-up |

## Patch series

`patches/pr-33`, `patches/pr-28`, `patches/pr-23`, `patches/pr-22` and `patches/pr-21` are `git format-patch` series against upstream `b3fda03`. All were checked by applying them to a fresh checkout of that commit: the resulting tree is identical to the tree of the PR branch on the fork. A `BASE.txt` in each says which head it is. When upstream merges a PR, delete its folder and its row here. `patches/pr-19` is against `67b2b38` instead, because it was made after upstream `main` had moved (its `BASE.txt` says how it was checked), and it has a second copy in `KeyedClick/` that is maintained beside it until the pull request lands.

`pr-21` is the one kept in step by hand: whenever `feat/clear-the-screen` moves, delete the old files in its folder, run `git format-patch b3fda03..<head> -o Upstream/patches/pr-21` from a checkout of the branch (use the new base if upstream `main` has moved), rewrite `BASE.txt`, apply the series to a fresh checkout of the base and check that its tree equals the branch head's, and update the row above.

## Not here

Recording unnamed controls (`ButtonImage`, `ButtonInvisible`, `Checkbox`) so a click lost to one can be
explained: a design choice for upstream, raised once as a suspicion for the Work Tab case and not supported by
that case's numbers. It stays a possible issue for Virginie to decide.
