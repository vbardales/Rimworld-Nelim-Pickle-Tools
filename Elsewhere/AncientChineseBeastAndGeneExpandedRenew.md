# Ancient Chinese Beast And Gene Expanded Renew

Inventory review, 2026-09-22: this suite has eight feature files and
`Tests/Pickle/Source/CriticalHookSteps.cs` with local steps for critical mod hooks. The current
source and feature counts are in `INVENTORY.md`.

The features cover loaded content, beast review, save/reload, critical hooks, incidents, chicken
crow, debug actions and recipes. Inspect `CriticalHookSteps.cs` before treating any step as a
generic extraction candidate; this inventory records source presence only, not a runtime pass.
